default: &default
  adapter: postgresql
  encoding: unicode
  url: <%= ENV.fetch("DATABASE_URL") { "postgres://postgres:postgres@localhost:5432/postgres" } %>

development:
  <<: *default
  database: finverse_development

test:
  <<: *default
  database: finverse_test
